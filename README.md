#slay the spire elden ring mod

Slay the Spire BaseMod character mod scaffold for `tarnished:`.

## Local build setup

Place these jars in `lib/` before building:

- `TarnishedMod.jar`
- `ModTheSpire.jar`
- `BaseMod.jar`

Then run:

```powershell
gradle jar
```


